CREATE TABLE "person" (
    id INT PRIMARY KEY,
    addressid INT,
    firstname VARCHAR(255),
    email VARCHAR(255),
    phone VARCHAR(15),
    age INT,
    gender VARCHAR(10),
    updated_at BIGINT
);